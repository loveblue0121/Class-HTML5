window.addEventListener("load", doFirst);
//window物件，load事件

function doFirst(){
    let canvas = document.getElementById("canvas");
    let context = canvas.getContext("2d"); //告訴他 canvas是2D的繪圖環境
/*    
    //context.fillStyle='color | gradient | pattern'; 顏色
    context.fillStyle='lightblue'; //填色顏色
    context.strokeStyle='blue'; //框線
    context.lineWidth=5; //邊框的寬 (不可用px)
    
    

    context.fillRect(100, 100, 300, 200);//填滿
    //context.strokeRect(100, 100, 300, 200); //矩形描邊
    context.clearRect(150, 150, 50, 50);
    
    context.rect(700, 600, 300, 200); //填滿
    context.fill();
    context.stroke();
    

    //橡皮擦 (寬, 高)
    //context.clearRect(0, 0, canvas.width, canvas.height);
*/    
    
    
    
}